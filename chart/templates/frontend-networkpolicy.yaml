{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "phasor.frontend.fullname" . }}
  labels:
    {{- include "phasor.frontend.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "phasor.frontend.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - ports:
        - protocol: TCP
          port: 8081
    {{- if .Values.networkPolicy.frontend.additionalIngress }}
    {{- toYaml .Values.networkPolicy.frontend.additionalIngress | nindent 4 }}
    {{- end }}
  egress:
    - to:
        - podSelector:
            matchLabels:
              {{- include "phasor.backend.selectorLabels" . | nindent 14 }}
      ports:
        - protocol: TCP
          port: 8080
    - to:
        - namespaceSelector: {}
          podSelector:
            matchLabels:
              k8s-app: kube-dns
      ports:
        - protocol: UDP
          port: 53
    {{- if .Values.networkPolicy.frontend.additionalEgress }}
    {{- toYaml .Values.networkPolicy.frontend.additionalEgress | nindent 4 }}
    {{- end }}
{{- end }}
